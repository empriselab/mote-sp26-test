# mote-sp26-test
directory to test the lrr-fa24-beta homeworks on Mote framework

before building docker, open the .env file and change it to your robot's IP.
# we should make a way to get the ip without having to connect a probe and run just 
build docker container via `docker build .` or navigate to this folder in vs code and hit `ctrl+shift+p` then search and select "Rebuild and Reopen in container"
